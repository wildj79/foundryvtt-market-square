/**
 * The Market Actor which represents the stores and shops that a pc would by 
 * weapons, armor, and other equipment from.
 * 
 * @extends {Actor}
 * 
 * @see {@link MarketSheet} 
 */
export class Market extends Actor {
    constructor(...args) {
        super(...args);
    }
}